//TODO: use boost.asio for api

// TODO: create class DB with static members and connection (postgresql???)

// TODO: add connection properies to config

// class GameController have ptr to DB connection (initiate on start)

// que<command> of commands which call api methods with same signature => int nameOfMethod(jsonData);